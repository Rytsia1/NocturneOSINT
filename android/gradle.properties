org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true

# Nocturne backend base URL (must end with /). 10.0.2.2 is the host machine as
# seen from the Android emulator. Override without editing this file:
#   ./gradlew assembleDebug -Pnocturne.apiBaseUrl=http://192.168.1.20:8080/
# or set nocturne.apiBaseUrl in ~/.gradle/gradle.properties.
nocturne.apiBaseUrl=http://10.0.2.2:8080/
